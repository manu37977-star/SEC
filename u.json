{
  "class_uid": 5001,
  "class_name": "Device Inventory Info",
  
  "category_uid": "Integer: Required: Numeric category UID as defined by OCSF.",
  "device": {
    "hostname": "String: Required: Primary hostname of the endpoint.",
    "uid": "String: Recommended: The unique identifier of the device. For example the Windows TargetSID or AWS EC2 ARN.",
    "name": "String: Optional: The alternate device name ordinarily as assigned by an administrator.",
    "ip": "String: Recommended: Primary IP address of the endpoint.",
    "type": "String: Optional: Endpoint platform or form factor e.g. Workstation, Laptop, Server, Mobile, VM.",
    "os": {
      "name": "String: Optional: Operating system name e.g. Windows, Linux, macOS.",
      "version": "String: Optional: Operating system version or build e.g. 11.0.22631, 22.04."
    },
    "owner": "user.json",
    "org": {
      "name": "String: Optional: Name of the organization or tenant the endpoint belongs to."
    },
    "agent_list": "Array: Optional: A list of agent objects associated with a device endpoint or resource.",
    "interface_name": "String: Recommended: The name of the network interface e.g. eth1.",
    "interface_uid": "String: Recommended: The unique identifier of the network interface.",
    "os_machine_uuid": "String: Optional: UUID of operating system assigned Machine ID.",
    "is_managed": "Boolean: Optional: Indicates whether the endpoint is managed by corporate tooling. True = managed, False = unmanaged.",
    "created_time": "String: Optional: Time when the endpoint record was created in ISO 8601 format.",
    "last_seen_time": "String: Optional: Last time the endpoint checked in or reported telemetry in ISO 8601 format."
  },

  "security": {
    "disk_encryption": "Boolean: Recommended: Indicates whether full disk encryption is enabled e.g. FileVault on macOS, BitLocker on Windows, LUKS on Linux.",
    "security_update": "String: Recommended: Identifier of the latest OS security update or patch level applied to this endpoint e.g. OS build number or last installed KB/patch ID.",
    "security_update_history_time": "String: Optional: Timestamp of the most recent security update in ISO 8601 format.",
    "patch_update": "String: Recommended: Current OS patch compliance status e.g. Up to date, Outdated, Critical missing, Unknown.",
    "password_policy": "Boolean: Recommended: Indicates whether the endpoint complies with the organisation password policy."
  },

  "status": "String: Optional: Current operational status of the endpoint e.g. Online, Offline, Retired.",
  "status_id": "Integer: Optional: Normalized numeric status code for the endpoint.",
  "status_detail": "String: Optional: Additional context describing the current status or reason for non-compliance.",

  "metadata": {
    "version": "String: Required: OCSF schema version e.g. 1.0.0.",
    "uid": "String: Optional: Original record UID in the source system. If no relevant ID exists keep blank.",
    "logged_time": "String: Optional: Time this record was collected in ISO 8601 format.",
    "Source": {
      "name": "String: Required: Name of the platform this data came from e.g. Github,Jamf, Intune, CrowdStrike. Write the name directly, not a variable.",
      "device_managed": "Boolean: Optional: Indicates whether the endpoint is managed by corporate tooling (true = managed, false = unmanaged).",
    },
    "extensions": {
      "custom_attributes": {
        "comments": "Array: Optional: Array of objects with fields author, timestamp, text representing analyst comments.",
        "tlp": "Integer: Optional: Traffic Light Protocol classification. Default: 2.",
        "custom_fields": "Object: Optional: Arbitrary key-value pairs for platform-specific or workflow-specific fields.",
        "assignee": "String: Optional: Email or identifier of the analyst responsible for reviewing this endpoint."
      }
    }
  }
}
